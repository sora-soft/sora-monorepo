{
  "name": "@sora-soft/etcd-discovery",
  "version": "1.4.2",
  "description": "sora system etcd discovery",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tspc",
    "clean": "rimraf ./dist",
    "prepublishOnly": "tspc",
    "prepare": "ts-patch install -s",
    "lint": "eslint",
    "preversion": "eslint && npm run build"
  },
  "repository": "git@github.com:sora-soft/etcd-discovery.git",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "sora",
    "etcd",
    "discovery"
  ],
  "author": "yaya",
  "license": "WTFPL",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "ts-patch": "^3.3.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@sora-soft/etcd-component": "workspace:*",
    "@sora-soft/framework": "workspace:*",
    "@sora-soft/typia-decorator": "workspace:*"
  }
}
